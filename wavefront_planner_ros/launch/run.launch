<launch>

  <!-- Wavefront planner -->
  <include file="$(find wavefront_planner)/launch/wavefront_planner.launch" />

  <!-- Rviz -->
  <node pkg="rviz" type="rviz" name="rviz_wavefront_planner"
    args="-d $(find wavefront_planner)/launch/rviz/wavefront_planner.rviz" />

</launch>